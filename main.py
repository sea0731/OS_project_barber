import threading
import time
import barber
import client
import numpy as np

def main:
    global mBarber = input("input barber: ")
    global Clientp = input("input cient: ")
    
    
    for i in range(1, mBarber)
        thread = barber.Barber("barber" + str(i))
        BarberThreads


    while True:
        sleep(np.random.poisson(Clientp, 1))
        

